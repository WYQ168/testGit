server:
  port: 8848
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/WYQ168/SpringCloud.git

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9999/eureka/